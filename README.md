
  # MailSense Dashboard UI Design

  This is a code bundle for MailSense Dashboard UI Design. The original project is available at https://www.figma.com/design/Ce1HbxzuW1HGJOUszd7lxZ/MailSense-Dashboard-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  